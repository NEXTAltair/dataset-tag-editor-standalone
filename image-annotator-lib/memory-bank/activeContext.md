# memory-bank/activeContext.md (Updated: 2025/04/02)

## 現在のタスク状況

- **完了:**
  - コアモジュール (`core/`, `api.py`, `exceptions/`) の主要な実装。
  - Tagger および Scorer モデルクラスの `models/` への移植とクラス階層への適合。
  - 主要 API (`annotate`) および設定ファイル (`annotator_config.toml`) のリネーム。
  - `docs/` ディレクトリのドキュメントを Diátaxis フレームワークに基づいて整理・統合。
  - `memory-bank` 内の関連ファイル (`productContext.md`, `decisionLog.md`, `progress.md`) を更新。
- **現在のフェーズ:** ドキュメント整理完了。次のステップへ移行準備中。

## 次のステップ (残りの作業)

`memory-bank/progress.md` に記載されている残りの作業項目に基づき、以下のタスクを進める必要があります。

1.  **テスト実装・実行:**
    - 単体テスト、結合テスト (BDD 含む) の作成・実行。
    - API 変更、pHash ベースの戻り値に対応したテスト修正・拡充。
    - カバレッジ確認。
2.  **ドキュメント最終化:**
    - 作成・移植したドキュメントの内容レビューとコード実装との整合性確認。
    - Docstring の網羅性と正確性の確認。
    - 必要に応じて図の追加。
    - 日本語 README (`README-JP.md`) の作成/更新。
3.  **依存関係整理:**
    - `pyproject.toml` の最終確認。
4.  **動作確認:**
    - 実際の利用環境での動作確認。
5.  **モデル実装確認:**
    - `ImageRewardScorer` の実装状況確認とドキュメント修正。
    - モデルのデフォルトパラメータ確認とドキュメント追記。

## 注意・確認事項 (継続中)

以下の点は、今後のテストやレビューで重点的に確認が必要です。

- Mypy エラーの完全な解消。
- 各モデルのエラーハンドリング (特に OOM) の適切性。
- `ModelLoad` のキャッシュ戦略 (特にメモリ逼迫時の挙動) の堅牢性。

# アクティブな作業コンテキスト

## 現在の焦点

- ModelLoad の階層構造リファクタリング
- BDD テストの構造化

## 作業中の主要コンポーネント

1. ModelLoad

   - 基底ローダー層の実装
   - 具象ローダー層の実装
   - メモリ管理機能の改善

2. テスト
   - BDD シナリオの更新
   - 新しい階層構造に対応したテストケース

## 注意点

- 既存の機能を維持しながらの段階的な移行
- 各モデルタイプの特性を考慮した実装
- メモリ管理の堅牢性確保

## 次のアクション

1. BaseModelLoader の実装開始
2. 最初の具象ローダー（TransformersLoader）の実装
3. テストケースの更新
